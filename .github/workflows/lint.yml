name: lint

on:
  pull_request:
    branches: [main]
    paths: ['README.md']

permissions:
  contents: read

jobs:
  awesome-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Run awesome-lint
        run: npx awesome-lint
